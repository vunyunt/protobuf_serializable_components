name: protobuf_serializable_components
description: Pure Dart serializable component serialization registry and mixins.
version: 1.0.0

environment:
  sdk: ^3.12.1

dependencies:
  meta: ^1.11.0
  protobuf: ^6.0.0
  fixnum: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
